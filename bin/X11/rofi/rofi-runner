#!/bin/sh

# Import Current Theme
. "$__distro_path_lib"
. "${__distro_path_root}/lib/common/WM"
. "${Distro_config_file}"

RASI="$HOME/.config/rofi/$ROFI_STYLE/runner.rasi"

# Run
rofi \
    -show run \
	-theme ${RASI}
