#!/bin/sh
# if this line exist script will be part of hub script.
. "$__distro_path_lib"
. "${__distro_path_root}/lib/common/WM"
. "${Distro_config_file}"

rofi -no-lazy-grab -show window -theme "$HOME/.config/rofi/$ROFI_STYLE"/windows.rasi 2>/dev/null
