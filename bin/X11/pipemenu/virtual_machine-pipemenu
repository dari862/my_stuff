#!/bin/sh
. "/usr/share/my_stuff/lib/common/pipemenu"

# Pipemenu
menuStart
if command -v virtual_machine >/dev/null 2>&1;then
	menuItem 'Run virtual machine' 'virtual_machine'
else
	cd /usr/share/my_stuff/Distro_Specific/apps_center/hypervisors
	for hy in *;do
		menuItem "install $hy" "popup_terminal --install $hy"
	done
fi
menuEnd
