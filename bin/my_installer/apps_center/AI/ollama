#!/bin/sh
say 'Installing ollama ...' 1
mkdir -p /tmp/$USER/ollama
getURL 'download2' 'https://ollama.com/install.sh' "/tmp/$USER/ollama/install.sh" || continue
sh /tmp/$USER/ollama/install.sh  || continue
