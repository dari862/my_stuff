#!/bin/sh
. "$__distro_path_lib"

stop() {
	"${__distro_path_root}"/bin/WM/kill_process.sh emacs
}

start() {
  setsid /usr/bin/emacs --daemon &
}

case "${1}" in
	""|start|restart)
		stop
		start		
	;;
	stop)	
		stop	
	;;
	*)	
		echo "Usage: emacs-session [start|stop|restart]"
	;;
esac
