#!/bin/sh
# need superuser : called by my-installer

app_name="secureMyLinux"
secureMyLinux_dir="$__distro_path_git/${app_name}"

say "clone https://${remote_repo_link}/${app_name}.git"
super_clone_repo "https://${remote_repo_link}/${app_name}.git" "${secureMyLinux_dir}"

say "Installing ${app_name}..." 1
generate_never_remove_bin "${secureMyLinux_dir}"/secureMyLinux.sh
my-superuser ${__distro_path_root}/system_files/bin/secureMyLinux.sh --full
