#!/bin/bash
set -e

dry_run=false
unmounted_devices=""

get_uuid() {
    blkid "$1" | sed -n 's/.* UUID="\([^"]*\)".*/\1/p'
}

get_fs_type() {
    blkid "$1" | sed -n 's/.*TYPE="\([^"]*\)".*/\1/p'
}

Parse_arguments(){
	case "$arg" in
	--dry-run)
		dry_run=true
		;;
	*)
		show_m "❌ Unknown option: $arg"
		show_m "Usage: $0 [--dry-run]"
		exit 1
	;;
	esac
}

fill_unmounted_devices_var(){
	_devices="$(lsblk -lnpo NAME,TYPE | awk '$2 == "disk" {print $1}')"
	mount_output="$(mount)"
	for dev in ${_devices};do
		if ! printf "%s\n" "$mount_output" | grep -q "$dev";then
			unmounted_devices="$unmounted_devices $dev"
		fi
	done

	if [ -z "$unmounted_devices" ]; then
		show_m "✅ No unmounted partitions found."
		exit 0
	fi
}

get_uuid_and_fs_type(){
	device_path=$(awk '{print $1}' <<< "$selected_line")

	if [ -z "$device_path" ] || [ ! -b "$device_path" ]; then
		show_m "❌ Invalid selection or device not found."
		exit 1
	fi

	uuid=$(get_uuid "$device_path")
	fs_type=$(get_fs_type "$device_path")

	if [ -z "$uuid" ] || [ -z "$fs_type" ]; then
		show_m "❌ Could not retrieve UUID or filesystem type for $device_path."
		exit 1
	fi

	if grep -q "$uuid" /etc/fstab; then
		show_m "⚠️ UUID already exists in /etc/fstab. Skipping."
		exit 0
	fi
}

check_mount_path_and_fs_type(){
	if [ -z "$mount_path" ]; then
		mount_path="/mnt/disk-${uuid:0:8}"
		show_m "ℹ️ Mount point auto-generated: $mount_path"
	fi

	case "$fs_type" in
		ext4|xfs) options="defaults" ;;
		btrfs)    options="defaults,compress=zstd" ;;
		ntfs)     options="defaults,uid=1000,gid=1000" ;;
		vfat)     options="defaults,uid=1000,gid=1000,umask=022" ;;
		*)        options="defaults"
				  show_m "⚠️ Unknown filesystem type '$fs_type'. Using default options." ;;
	esac
}

run_automount(){
	if [ "$dry_run" = true ]; then
		show_m "🚫 Dry-run mode active — no changes will be made."
		exit 0
	fi

	mkdir -p "$mount_path"

	backup_file="/etc/fstab.backup.$(date +%F-%H%M%S)"
	cp /etc/fstab "$backup_file"
	show_m "📁 fstab backed up to $backup_file"

	{
		printf "# Added by automount script on %s\n" "$(date)"
		printf "UUID=%s %s %s %s 0 2\n" "$uuid" "$mount_path" "$fs_type" "$options"
	} | tee -a /etc/fstab

	if mount "$mount_path"; then
		show_m "✅ Mounted $device_path at $mount_path"
		df -h | grep --color=never "$mount_path"
	else
		show_m "❌ Failed to mount $device_path."
		exit 1
	fi
}
